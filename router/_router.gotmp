// Code generated by go-gen. DO NOT EDIT.
// 由 go-gen 生成的代码，请勿编辑。

package {{ .PackagePathName }}

import (
	"{{ .PackageBaseName }}/routers"
)

//function
func init() {
	{
		{{- range $f := .Functions }}
		{{- range $s := $f.Paths}}
		routers.DefaultRouter.Router("{{ $f.GroupPath }}", {{ $f.Name }}, "{{ $s.PathMethod }}")
		{{- end -}}
		{{- end}}
	}
}

//struct function
func init() {
	{
		{{- range $f := .StructFunctions }}
		{
			p := &{{ $f.StructName }}{}
			{{- range $i,$fn := $f.Functions}}
			h{{ $i }} := routers.ToBindF(p, p.{{ $fn.Name }}, "{{ $fn.Name }}")
			{{- range $s := $fn.Paths}}
			routers.DefaultRouter.Router("{{ $fn.GroupPath }}", h{{ $i }}, "{{$s.PathMethod}}")
			{{- end -}}
			{{- end}}
		}
		{{- end}}
	}
}
